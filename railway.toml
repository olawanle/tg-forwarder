[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# Leave startCommand empty / unset in the Railway UI.
# A dashboard start command that uses bare $PORT will crash Streamlit.
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
